import { StyledHeader } from "./styles/Header.styled"

const Header = () => {
    return (
        <StyledHeader>
            <h1>
                the climb shop
            </h1>
        </StyledHeader>
    )
}

export default Header